--- a/skills/research/evidence-first-content-research/SKILL.md
+++ b/skills/research/evidence-first-content-research/SKILL.md
@@ -17,10 +17,11 @@
 
 ### Stage 1 — 原文速读
 
-- Default for a single article/link with “研究一下 / 调研一下 / 怎么看 / 看看”: use the current Hermes agent, acquire the actual source via existing extraction/cache, and give a short source readout. No automatic specialist delegation or broad external search campaign. A user explicitly requesting a specialist still controls that choice.
+- Default for a single article/link with “研究一下 / 调研一下 / 怎么看 / 看看”: use the current Hermes agent, acquire the actual source via existing extraction/cache, and give a compact analytical readout, not a teaser. No automatic specialist delegation or broad external search campaign. A user explicitly requesting a specialist still controls that choice.
 - Target the first useful readout within 60 seconds when source access permits. This is a service target, not a measured guarantee or permission to invent content. Use bounded extraction and a necessary fallback; if inaccessible, report the real access/coverage blocker promptly, not a fabricated summary or a bare started acknowledgement.
-- Include title/source, one-sentence thesis, 3–5 author claims, a bounded preliminary assessment, material evidence gaps and 2–3 recommended research directions. Mark “基于原文，未完成外部交叉核验”. Do not call author statements independently verified facts. Preserve dates, units and scope; check obvious load-bearing arithmetic with execute_code. High-risk advice and current actionable recommendations need appropriate verification, not an unverified affirmative recommendation with a disclaimer.
+- Restore analytical depth: lead with a qualified judgment, then explain mechanisms/causal chains, concrete implications or comparisons, misconceptions and counterexamples, applicability boundaries and actionable advice. A substantive source often warrants roughly 1000–1800 Chinese characters, but never pad thin material or enforce a fixed length. Separate source claims from analytical inference; a compact analysis is not a verified full investigation. A small targeted primary-source check may support a load-bearing claim, but do not launch broad research. Put gaps and 2–3 further directions last; they must not replace the analysis. Optimize redundant fetching, retries and duplicate generation, not useful reasoning. Mark “基于原文，未完成外部交叉核验”. Do not call author statements independently verified facts. Preserve dates, units and scope; check obvious load-bearing arithmetic with execute_code. High-risk advice and current actionable recommendations need appropriate verification, not an unverified affirmative recommendation with a disclaimer.
 - Do not search an opaque URL identifier, load unrelated history, produce a long hidden report before summarizing, or invoke research_deposit/recover/status solely to finish this readout. A quick readout is not an adopted complete research artifact and creates no pre-answer deposit requirement. Keep no_save and all host consent boundaries intact.
+- Video source handling: for Douyin go directly to browser_exec, not article-shell extraction. Batch identity, playback availability and captions/tracks metadata in one call. Use an available authorized transcript workflow or bounded local frame extraction; never assume frames are a transcript. No guessed detail APIs, speech-model downloads or repeated screenshots of an obstructed player on the quick path. Stop at a real login/access wall. Return browser screenshots explicitly; resolve workspace from the actual tool result or BH_AGENT_WORKSPACE, not a guessed variable. Validate one frame before further sampling. Use an approved local-media tool/execute_code path, never browser Python to bypass a denied command. Disclose content coverage and distinguish topic analysis from video analysis.
 - End with useful continuation choices, then stop. Silence does not authorize deeper work. This stage is not the completion of an explicitly requested full investigation.
 
 ### Stage 2 — 按需深研
--- a/skills/research/article-research-summary/SKILL.md
+++ b/skills/research/article-research-summary/SKILL.md
@@ -22,10 +22,11 @@
 
 ### Stage 1 — 原文速读
 
-- Default for a single article/link with “研究一下 / 调研一下 / 怎么看 / 看看”: use the current Hermes agent, acquire the actual source via existing extraction/cache, and give a short source readout. No automatic specialist delegation or broad external search campaign. A user explicitly requesting a specialist still controls that choice.
+- Default for a single article/link with “研究一下 / 调研一下 / 怎么看 / 看看”: use the current Hermes agent, acquire the actual source via existing extraction/cache, and give a compact analytical readout, not a teaser. No automatic specialist delegation or broad external search campaign. A user explicitly requesting a specialist still controls that choice.
 - Target the first useful readout within 60 seconds when source access permits. This is a service target, not a measured guarantee or permission to invent content. Use bounded extraction and a necessary fallback; if inaccessible, report the real access/coverage blocker promptly, not a fabricated summary or a bare started acknowledgement.
-- Include title/source, one-sentence thesis, 3–5 author claims, a bounded preliminary assessment, material evidence gaps and 2–3 recommended research directions. Mark “基于原文，未完成外部交叉核验”. Do not call author statements independently verified facts. Preserve dates, units and scope; check obvious load-bearing arithmetic with execute_code. High-risk advice and current actionable recommendations need appropriate verification, not an unverified affirmative recommendation with a disclaimer.
+- Restore analytical depth: lead with a qualified judgment, then explain mechanisms/causal chains, concrete implications or comparisons, misconceptions and counterexamples, applicability boundaries and actionable advice. A substantive source often warrants roughly 1000–1800 Chinese characters, but never pad thin material or enforce a fixed length. Separate source claims from analytical inference; a compact analysis is not a verified full investigation. A small targeted primary-source check may support a load-bearing claim, but do not launch broad research. Put gaps and 2–3 further directions last; they must not replace the analysis. Optimize redundant fetching, retries and duplicate generation, not useful reasoning. Mark “基于原文，未完成外部交叉核验”. Do not call author statements independently verified facts. Preserve dates, units and scope; check obvious load-bearing arithmetic with execute_code. High-risk advice and current actionable recommendations need appropriate verification, not an unverified affirmative recommendation with a disclaimer.
 - Do not search an opaque URL identifier, load unrelated history, produce a long hidden report before summarizing, or invoke research_deposit/recover/status solely to finish this readout. A quick readout is not an adopted complete research artifact and creates no pre-answer deposit requirement. Keep no_save and all host consent boundaries intact.
+- Video source handling: for Douyin go directly to browser_exec, not article-shell extraction. Batch identity, playback availability and captions/tracks metadata in one call. Use an available authorized transcript workflow or bounded local frame extraction; never assume frames are a transcript. No guessed detail APIs, speech-model downloads or repeated screenshots of an obstructed player on the quick path. Stop at a real login/access wall. Return browser screenshots explicitly; resolve workspace from the actual tool result or BH_AGENT_WORKSPACE, not a guessed variable. Validate one frame before further sampling. Use an approved local-media tool/execute_code path, never browser Python to bypass a denied command. Disclose content coverage and distinguish topic analysis from video analysis.
 - End with useful continuation choices, then stop. Silence does not authorize deeper work. This stage is not the completion of an explicitly requested full investigation.
 
 ### Stage 2 — 按需深研
