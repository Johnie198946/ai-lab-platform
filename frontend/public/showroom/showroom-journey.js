(() => {
  'use strict';
  const layers = ['WATT', 'FLOPS', 'TOKENS', 'AGENTS', 'VALUES'];
  const screens = [
    ['S0', '访前备课台', '主持人如何把已核验事实与待客户纠正的假设分开？', '生成可证伪的来访备课包，不把后台画像带入客户现场。', 'VisitBrief：事实、假设、角色关注点、启发问题与禁用台词', '客户与行业洞察、政策市场、竞争替代、账户综合、证据边界 Agent；输入授权资料，输出带来源和置信度的草案，人工决定对外口径。', '客户语境隔离、授权材料、角色差异化表达、复访双门禁。', '只形成容量、数据本地性、模型规模、SLA和现有资源的追问清单，不做硬件推荐。', '把设备讨论延后到客户事实和运行基线可核验之后。', 'SIMULATION'],
    ['S1', '欢迎与制造业全景', '今天为什么值得看，001是实践还是概念？', '用批准的制造业全景图建立共同语境，共同验证实践、IPD产品化与Token Factory的关系。', '制造业解决方案全景图与三条总导航：001实践、Token Factory证据、客户需求共创', '叙事导航 Agent；输入客户角色和原图证据，输出3分钟或10分钟讲解路径，不能把架构展示说成全面上线。', '建立信任，说明AI进入经营流程，而不是孤立聊天。', '首次展示 WATT → FLOPS → TOKENS → AGENTS → VALUES，并把算力解释为AI生产能力承载。', '边缘、试点、共享与中心形态只作方向提示，正式型号与范围需后续核验。', 'SIMULATION'],
    ['S2', '001自身制造实践全景', 'AI进入了哪些真实业务对象，为什么通用大模型不够？', '把研发、供应、计划、质量、设备、物流、成本、订单和售后串成经营链。', '业务域—系统—Agent动作—知识数据—人工决策点映射', '研发创新、供应韧性、智能排产、缺陷检测、预测维保、物流风险、成本风控、市场洞察 Agent；默认只读，没有回写证据就标UNCONNECTED。', '异构系统接入、制造知识、冲突处理、数据不出域、持续运行和经营反馈。', 'FusionOne AI统一纳管、XPU Engine调度、TokenOps配额计量，以及按业务域关联模型与上下文。', '让本地推理、长上下文、多任务并发和高显存需求有可解释的承载方向，不承诺部署规模。', 'SIMULATION'],
    ['S3', '客户需求多轮澄清', '这些实践与我有什么关系，我们真正卡住的是哪一步？', '保留客户原话，同时结构化业务场景、目标、多方诉求、约束与验收条件。', 'CustomerDemand draft：原始输入与结构化需求并存', '需求澄清 Agent；输入聊天原文和角色，输出事实、假设、TBD、需求项与冲突，不能代客户裁决优先级。', '单一需求入口、多方诉求不覆盖、租户和创建人由服务端派生、同一来源防重复。', '补齐并发、输入输出长度、时延、数据位置、现有资源与峰谷等选型变量，不输出台数。', '将容量、数据本地性和现有资源盘点变成可核验问题。', 'UNCONNECTED'],
    ['S4', '需求收敛单与001演示切片', '我们达成了什么共识，演示是否等于立项？', '逐项确认或保留TBD，物理区分客户真实诉求与001演示候选切片。', 'CustomerDemand confirmed：版本化需求收敛单与可追溯验收标准', '需求建模、冲突识别、演示切片选择、证据守界 Agent；冲突原样保留，人工确认后才能锁定需求。', '需求基线、版本、确认状态、唯一ID与验收标准先于实现。', '建立“需求项 → 验收信号 → 成功任务 → Token成本 → 业务价值”锚点。', '没有成功任务定义，就不做容量和采购测算；候选硬件只能待确认。', 'UNCONNECTED'],
    ['S5', 'IPD概念：值不值得做', '哪些需求该接纳、退回、待定或拒绝？', '用市场、客户、竞争、技术和商业交付证据做有条件判断，展示反证与缺证阻断。', '市场机会包、需求基线、证据/反证与accept/conditional/return/reject建议', '五类研究 Agent、IPD-01综合、概念主审、证据核验、边界审查、IPD-02评审 Agent；综合不能删除冲突。', '多源证据、并行隔离、来源置信度、可回放、人工材料入口，不自动批准。', '智能模型路由、统一Token计量、模型目录与配额优先级。', '并发研究、模型大小和数据本地性开始形成算力基线，但不急于定台数。', 'SIMULATION'],
    ['S6', 'IPD计划：做什么与资源承诺', '产品范围、架构、容量、成本和采购怎么定？', '把需求转成产品包、非目标、架构、依赖、验收矩阵和资源计划。', '初始产品包、架构候选、容量假设、采购测算草案与TBD', 'IPD-03产品规划、IPD-04系统架构、IPD-09合规预检、容量与硬件测算 Agent；输出草案，PDCP/TR2/TR3仍由人负责。', '容量估算、模型收敛、异构适配、数据边界、预算、冗余与扩容依据。', '路由、精选模型目录、Day0适配、配额和“峰值Token/s ÷ 单卡实测SLA”可解释换算。', 'FusionXpark/TokenBox、FusionOne AI一体机/超融合、G5580 X+高速互联/TokenFabric/液冷整机柜均为条件式候选，待实测与正式配置。', 'SIMULATION'],
    ['S7', 'IPD开发：如何成为可执行产品', '方案、代码和可交付产品之间如何建立证据链？', '展示规格、实现、集成测试、构建证据和一次失败返工，而不是把生成方案说成已开发。', '接口合同、实现任务包、受控执行轨迹、构建与测试证据', 'IPD-05/06/07、受控代码Worker、执行追踪 Agent；工具、沙箱、版本和人工批准边界可见。', '依赖调度、受控工具、幂等、断点恢复、失败隔离、模型/工具/知识版本锁定。', '智能路由、XPU Engine异构纳管、推理优化、边缘执行和按Agent计量。', '敏感数据可就近执行，共享底座可统一模型和容器；未接真实Worker时明确SIMULATION/UNCONNECTED。', 'SIMULATION'],
    ['S8', 'IPD验证：稳定、合格、可复制', '演示跑一次，凭什么能进生产？', '用需求验收标准串起规格、压力、质量、安全、缺陷闭环和发布建议。', '绑定版本、环境、卡型、模型、数据集和时间的验证证据包', 'IPD-08验证、IPD-09合规安全、PQA与缺陷闭环 Agent；只做证据检查，不签署结论。', '可重复性能基线、稳定性、互联、缺陷复测，以及任务成功率、时延、质量和成本联合验收。', '生产账、消费账、价值账三账校验，任务成功成本关联业务结果。', '只有真实单卡SLA吞吐与业务基线支持扩容、互联或液冷判断。', 'SIMULATION'],
    ['S9', '发布与生命周期：规模运营', '试点通过后如何复制、控预算并跟上模型演进？', '把“能跑”转为可运营的发布准备、配额、服务、模型生命周期和价值归因。', '发布准备包、MOR输入、运营基线与生命周期建议', 'IPD-10发布、IPD-11营销/MOR、IPD-12生命周期、Token运营与服务容量 Agent；EOP/EOM/EOS只生成建议。', '多部门/多租户配额、账单、预算告警与熔断、版本资产、健康和扩容依据。', '统一计量、分级预算预警、模型目录、Day0、Token到任务到业务结果归因。', '设备是持续AI产能的一部分，扩容由成功任务成本、峰值负载和冗余策略驱动。', 'SIMULATION'],
    ['S10', '客户建设建议、产品路径与带走物', '哪些机制能借鉴，我该先做什么、买什么？', '先转译机制，再做条件式产品适配，输出匹配、支撑性匹配、联合创新或无匹配。', '脱敏带走包：需求收敛单、001机制转译、阶段路线与待补参数', '机制转译、Token Factory映射、容量选型、成熟度核验、建设建议 Agent；回指已确认需求和证据，不复述产品资料。', '业务指标→运行指标→Token/模型→资源→阶段门，采购依据可追问、复算和回滚。', '统一入口、路由、计量、Day0、互联、三账与生命周期运营，按阶段逐步建设。', '本地/单场景候选TokenBox或FusionXpark；共享候选FusionOne AI一体机/超融合；高并发候选G5580 X+高速互联/TokenFabric/液冷整机柜。具体卡数、台数、比例和报价待补齐参数与压测。', 'SIMULATION'],
  ].map(([id, title, question, goal, artifact, agent, need, feature, value, evidenceStatus]) => ({ id, title, customerQuestion: question, screenGoal: goal, coreArtifact: artifact, agentDesign: agent, businessSystemNeed: need, tokenFactoryFeature: feature, hardwareCommercialValue: value, evidenceStatus }));
  const fields = [['business_scene', '业务场景'], ['overall_goal', '总体目标'], ['stakeholders', '相关方'], ['requirement_items', '需求项'], ['conflict_notes', '冲突与TBD'], ['constraints', '约束'], ['acceptance_criteria', '验收标准']];
  const state = { index: 0, demand: null, apiError: '', hermesStatus: 'idle', chat: '', sending: false }; const app = document.querySelector('#journey-app');
  const node = (tag, value, className) => { const item = document.createElement(tag); item.textContent = String(value ?? ''); if (className) item.className = className; return item; };
  const headers = () => { let token = ''; try { token = JSON.parse(localStorage.getItem('ai-lab-platform.auth') || '{}').accessToken || ''; } catch (_) {} return token ? { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' } : { 'Content-Type': 'application/json' }; };
  async function demandApi(path, method = 'GET', body) { const response = await fetch(`/api/v1/demands${path}`, { method, headers: headers(), body: body === undefined ? undefined : JSON.stringify(body) }); if (!response.ok) { const detail = await response.json().then((data) => data.detail).catch(() => ''); throw new Error(`CustomerDemand API ${response.status}${detail ? `：${detail}` : ''}`); } return response.json(); }
  function showError(error) { state.apiError = error.message || 'CustomerDemand API 不可用'; render(); }
  function demandPayload(form) { const data = { source_text: form.elements.source_text.value.trim() }; fields.forEach(([key]) => { const controls = [...form.querySelectorAll(`[name="${key}"]`)].map((input) => input.value.trim()).filter(Boolean); data[key] = ['stakeholders', 'requirement_items', 'conflict_notes', 'constraints', 'acceptance_criteria'].includes(key) ? controls : (controls[0] || ''); }); return data; }
  function demandForm(editable) { const form = document.createElement('form'); form.className = 'demand-form'; const values = state.demand || {}; const sourceLabel = node('label', '客户原话（原始输入与结构化结果同时保留）'); const source = document.createElement('textarea'); source.name = 'source_text'; source.required = true; source.maxLength = 12000; source.value = values.source_text || ''; source.readOnly = !editable; sourceLabel.append(source); form.append(sourceLabel); fields.forEach(([key, label]) => { const wrap = document.createElement('fieldset'); wrap.className = 'field-group'; wrap.append(node('legend', label)); const current = Array.isArray(values[key]) ? values[key] : [values[key] || '']; current.forEach((value) => { const input = key === 'business_scene' || key === 'overall_goal' ? document.createElement('textarea') : document.createElement('input'); input.name = key; input.maxLength = key === 'overall_goal' ? 1000 : 500; input.value = value; input.readOnly = !editable; wrap.append(input); }); form.append(wrap); }); return form; }
  function demandCard(screen) { const card = document.createElement('article'); card.className = 'card artifact'; card.append(node('h3', 'CustomerDemand · 服务端需求真源')); if (state.apiError) card.append(node('p', state.apiError, 'error-message')); if (screen.id === 'S3' && !state.demand) { const form = demandForm(true); const submit = node('button', '保存需求草案', 'primary'); submit.type = 'submit'; form.append(submit); form.addEventListener('submit', async (event) => { event.preventDefault(); try { state.demand = await demandApi('', 'POST', demandPayload(form)); history.replaceState(null, '', `?demand_id=${encodeURIComponent(state.demand.demand_id)}`); render(); } catch (error) { showError(error); } }); card.append(form); return card; } if (!state.demand) { card.append(node('p', '请先在 S3 保存需求草案，再回到 S4 回读。', 'muted')); return card; } const record = document.createElement('div'); record.className = 'demand-record'; ['demand_id', 'source_hash', 'version', 'status'].forEach((key) => record.append(node('span', `${key}: ${state.demand[key]}`, 'record-chip'))); card.append(record); const editable = state.demand.status !== 'confirmed' && screen.id === 'S4'; const form = demandForm(editable); if (editable) { const save = node('button', '保存结构化编辑', 'secondary'); save.type = 'submit'; form.append(save); form.addEventListener('submit', async (event) => { event.preventDefault(); try { state.demand = await demandApi(`/${encodeURIComponent(state.demand.demand_id)}`, 'PATCH', { ...demandPayload(form), expected_version: state.demand.version }); state.apiError = ''; render(); } catch (error) { showError(error); } }); } card.append(form); if (screen.id === 'S4') { const confirm = node('button', state.demand.status === 'confirmed' ? '已确认（人工门已通过）' : '人工确认需求单', 'primary'); confirm.type = 'button'; confirm.disabled = state.demand.status === 'confirmed'; confirm.addEventListener('click', async () => { try { state.demand = await demandApi(`/${encodeURIComponent(state.demand.demand_id)}/confirm`, 'POST', { expected_version: state.demand.version }); render(); } catch (error) { showError(error); } }); card.append(confirm); } return card; }
  function chatCard(screen) { const card = document.createElement('article'); card.className = 'card artifact chat'; card.append(node('h3', '聊天解释入口 · Hermes'), node('p', state.chat || (state.hermesStatus === 'error' ? 'Hermes 当前不可用' : '尚未请求解释；回复将来自真实 Hermes 会话。'), 'chat-output')); const button = node('button', state.sending ? 'Hermes 正在生成…' : `请 Hermes 解释「${screen.title}」`, 'secondary'); button.type = 'button'; button.disabled = state.sending || !window.showroomApi; button.addEventListener('click', async () => { state.sending = true; state.chat = ''; render(); try { await window.showroomApi.submitHermesPrompt(`请基于当前客户旅程屏 ${screen.id}「${screen.title}」解释客户问题、证据边界和Token Factory价值。只使用已知内容，不编造数据；如证据不足请明确说待核验。`, { stationContext: `${screen.customerQuestion}\n${screen.screenGoal}` }); } catch (error) { state.chat = `Hermes 当前不可用：${error.message}`; state.hermesStatus = 'error'; } finally { state.sending = false; render(); } }); card.append(button); const details = document.createElement('details'); details.className = 'drawer'; details.append(node('summary', '查看 Agent 输入、输出与人工门'), node('p', screen.agentDesign)); card.append(details); return card; }
  function render() { const screen = screens[state.index]; app.replaceChildren(); const shell = document.createElement('div'); shell.className = 'journey-shell'; const rail = document.createElement('aside'); rail.className = 'rail'; rail.append(node('div', 'AI LAB', 'logo'), node('p', '制造业客户认知旅程'), node('p', 'S0—S10 · Token Factory')); const nav = document.createElement('nav'); nav.className = 'steps'; nav.setAttribute('aria-label', '旅程导航'); screens.forEach((item, index) => { const button = node('button', '', `step${index === state.index ? ' active' : ''}`); button.type = 'button'; button.append(node('small', item.id), node('span', item.title)); button.addEventListener('click', () => { state.index = index; render(); }); nav.append(button); }); rail.append(nav); shell.append(rail); const main = document.createElement('main'); main.id = 'journey-main'; main.className = 'main'; if (state.demand?.status === 'confirmed') { const handoff = node('a', '进入 AI 员工共创', 'architect-handoff'); handoff.href = `/architect?customer_demand_id=${encodeURIComponent(state.demand.demand_id)}`; main.append(handoff); } const top = document.createElement('div'); top.className = 'topline'; top.append(node('div', 'CUSTOMER CO-CREATION / TOKEN FACTORY', 'eyebrow'), node('div', `证据状态 · ${screen.evidenceStatus}`, `status ${screen.evidenceStatus.toLowerCase()}`)); main.append(top); const layerBar = document.createElement('div'); layerBar.className = 'layers'; layers.forEach((item, index) => layerBar.append(node('span', item, `layer${index <= Math.min(4, Math.floor(state.index / 2)) ? ' on' : ''}`))); main.append(layerBar); const hero = document.createElement('section'); hero.className = 'card hero'; const copy = document.createElement('div'); copy.append(node('div', screen.id, 'eyebrow'), node('h1', screen.title), node('p', screen.customerQuestion)); if (screen.id === 'S1') { const image = document.createElement('img'); image.className = 'panorama'; image.alt = '超聚变制造业解决方案全景'; image.src = './assets/manufacturing-panorama.png'; copy.append(image); } hero.append(copy, node('p', `本页目标：${screen.screenGoal}`)); main.append(hero); const grid = document.createElement('div'); grid.className = 'content-grid'; [['客户问题', screen.customerQuestion], ['屏幕目标', screen.screenGoal], ['核心产物', screen.coreArtifact], ['业务 / 系统需求', screen.businessSystemNeed], ['Token Factory 特性', screen.tokenFactoryFeature], ['硬件 / 商业价值', screen.hardwareCommercialValue]].forEach(([label, value]) => { const card = document.createElement('article'); card.className = 'card contract'; card.append(node('h3', label), node('p', value)); grid.append(card); }); const evidence = document.createElement('article'); evidence.className = 'card artifact'; evidence.append(node('h3', '证据与人工边界'), node('p', `${screen.evidenceStatus} · 未经接入、授权、压测或人工确认的内容保持待核验，不显示为LIVE。`)); grid.append(evidence); if (screen.id === 'S3' || screen.id === 'S4') grid.append(demandCard(screen)); grid.append(chatCard(screen)); main.append(grid); const actions = document.createElement('div'); actions.className = 'footer-actions'; const previous = node('button', '上一屏', 'secondary'); previous.type = 'button'; previous.disabled = state.index === 0; previous.addEventListener('click', () => { state.index -= 1; render(); }); const next = node('button', state.index === screens.length - 1 ? '重新浏览' : '下一屏', 'primary'); next.type = 'button'; next.addEventListener('click', () => { state.index = state.index === screens.length - 1 ? 0 : state.index + 1; render(); }); actions.append(previous, next); main.append(actions); shell.append(main); app.append(shell); }
  window.showroomApi?.on('hermes-status', ({ status, detail }) => { state.hermesStatus = status; if (status === 'error' || status === 'auth-required') state.chat = detail || 'Hermes 当前不可用'; render(); }); window.showroomApi?.on('hermes-event', (event) => { const payload = event.payload || {}; if (event.type === 'message.delta') state.chat += String(payload.text || ''); if (event.type === 'message.complete') { state.chat = window.showroomApi.visibleAssistantMessage(String(payload.text || state.chat)); state.sending = false; } render(); });
  render(); const restoredDemandId = new URLSearchParams(location.search).get('demand_id'); if (restoredDemandId) demandApi(`/${encodeURIComponent(restoredDemandId)}`).then((demand) => { state.demand = demand; render(); }).catch(showError); if (window.showroomApi) window.showroomApi.init().catch((error) => { state.hermesStatus = 'error'; state.chat = `Showroom 服务不可用：${error.message}`; render(); });
})();
