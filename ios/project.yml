# AIPlatformApp - XcodeGen 工程配置
# 生成: xcodegen generate
name: AIPlatformApp
options:
  bundleIdPrefix: com.ailab
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.1"
  createIntermediateGroups: true

targets:
  AIPlatformApp:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - AIPlatformApp
    info:
      path: AIPlatformApp/Info.plist
      properties:
        CFBundleDisplayName: Quantum
        CFBundleIdentifier: $(PRODUCT_BUNDLE_IDENTIFIER)
        CFBundlePackageType: $(PRODUCT_BUNDLE_PACKAGE_TYPE)
        CFBundleShortVersionString: "1.0"
        CFBundleVersion: "1"
        CFBundleURLTypes:
          - CFBundleURLName: com.ailab.AIPlatformApp.oauth
            CFBundleURLSchemes:
              - quantum
        UILaunchScreen: {}
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UIBackgroundModes:
          - audio
        NSMicrophoneUsageDescription: "用于语音交互输入，将你的语音实时转写为文字。"
        NSSpeechRecognitionUsageDescription: "用于将你的语音转写为文字，辅助对话输入。"
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
          - UIInterfaceOrientationPortrait
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ailab.AIPlatformApp
        SWIFT_VERSION: "5.9"
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_IDENTITY: ""
        CODE_SIGNING_REQUIRED: NO
        CODE_SIGNING_ALLOWED: NO
        ASSETCATALOG_COMPILER_GENERATE_ASSET_SYMBOLS: YES
    scheme:
      testTargets:
        - AIPlatformAppTests

  AIPlatformAppTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - AIPlatformAppTests
    dependencies:
      - target: AIPlatformApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ailab.AIPlatformAppTests
        SWIFT_VERSION: "5.9"
        CODE_SIGN_IDENTITY: ""
        CODE_SIGNING_REQUIRED: NO
        CODE_SIGNING_ALLOWED: NO
